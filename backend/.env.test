# Test environment variables
# Used by integration tests

PORT=5001
NODE_ENV=test

# Use a separate test database to avoid polluting development data
MONGO_URI_TEST=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/farmigo_test?appName=Farmigo

# Use the same MONGO_URI as dev if you don't have a separate test DB
MONGO_URI=mongodb+srv://sineth:sineth1211@farmigo.hqgwt3w.mongodb.net/farmigo_test?appName=Farmigo

JWT_SECRET=test_jwt_secret_key_for_testing_only
JWT_EXPIRES_IN=1h

OPENWEATHER_API_KEY=your_test_api_key_here
